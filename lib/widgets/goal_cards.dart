import 'package:flutter/material.dart';

class GoalCards extends StatelessWidget {
  const GoalCards({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Long-term goals'),
      ),
    );
  }
}