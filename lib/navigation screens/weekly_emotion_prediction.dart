import 'package:flutter/material.dart';

class WeeklyEmotionPredictionScreen extends StatelessWidget {
  const WeeklyEmotionPredictionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('nothing to show'),
      ),
    );
  }
}
